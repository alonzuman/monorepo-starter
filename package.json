{
	"name": "mono",
	"version": "1.0.0",
	"private": true,
	"description": "",
	"main": "index.js",
	"scripts": {
		"web:start": "cd apps/web && npm run dev",
		"spa:start": "cd apps/spa && npm start",
		"dev": "concurrently \"npm run spa:start\" \"npm run web:start\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "Alon Zuman",
	"license": "ISC",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"devDependencies": {
		"concurrently": "^7.2.2"
	}
}
