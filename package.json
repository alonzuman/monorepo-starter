{
	"name": "mono",
	"version": "1.0.0",
	"private": true,
	"description": "",
	"main": "index.js",
	"scripts": {
		"web:dev": "cd apps/web && npm run dev",
		"spa:dev": "cd apps/spa && npm run dev",
		"server:dev": "cd apps/server && npm run dev",
		"dev": "concurrently \"npm run spa:dev\" \"npm run web:dev\" \"npm run server:dev\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "Alon Zuman",
	"license": "ISC",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"devDependencies": {
		"concurrently": "^7.2.2"
	}
}
